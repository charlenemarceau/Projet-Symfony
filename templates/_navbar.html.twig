
    <nav id="nav" class="navbar navbar-expand-lg navbar-light fixed-top" style="background-color: #d6c0d8;">
        <div class="container-fluid px-5">
          <a class="navbar-brand" href="{{ path( 'app_home')}}">Veggie&amp;Cook</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                {% if app.user %}
                    <div class="mt-2 mx-5">Bonjour {{ app.user.firstname }}, vous êtes bien connecté.</div>
                    <a class="nav-link" aria-current="page" href=" {{ path( 'recipe_new')}}">Nouvelle recette</a>
                    <a class="nav-link" href="{{ path( 'app_account')}}">Votre profil</a>
                    <a class="nav-link" href="{{ path( 'app_logout')}}">Déconnexion</a>
                {% else %}
                    <a class="nav-link" href="{{ path( 'app_register')}}">Inscription</a>
                    <a class="nav-link" href="{{ path( 'app_login')}}">Se connecter</a>
                {% endif %} 
                </div>
            </div>
        </div>
    </nav>