<div class="footer">
<div class="row">
        <div class="left col-md-4">
            Catégories
            <ul>
        {% for c in categories %}
                <li>
                <a href="{{ path('app_recipe_category', {'id': c.id} ) }}">{{c.name}}</a>
          {# <a class="dropdown-item" href="{{ path('app_recipe_category', {'id': c.id} ) }}">{{cat.name}}</a> #}
                </li>
        {% endfor %}
        
            </ul>
        </div>
        <div class="right col-md-6">
            <p>Tu aimes cuisiner et partager ? <a href="{{ path('app_register')}}">Rejoins nous</a> </p>
        </div>
    </div>
</div>