{% extends 'base.html.twig' %}

{% block title %}Nouvelle recette{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto">
    <h1>Partage une recette</h1>

    {{ include('recipe/_form.html.twig') }}
        
        </div>
    </div>

    <a href="{{ path('app_home') }}">back to list</a>
{% endblock %}
