<form method="post" action="{{ path('recipe_delete', {'id': recipe.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cette recette ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recipe.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
{# <form class="deleteform" action="{{ path('app_todo_delete_csrf', { id: todo.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE">
            <input type="hidden" name="token" value="{{ csrf_token('delete-item') }}"/>
            <button type="submit" class="btn btn-danger">Supprimer</button>
        </form> #}